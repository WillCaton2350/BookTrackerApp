# BookTrackerApp
Written in Python and Django, this application allows the user to download multiple books from an e-library they build through the api and post reviews to an html page via input form. 
The Uploads dir needs to be in the frontend dir, the images dir needs to be in the static dir and the env needs to be in the backend dir.
